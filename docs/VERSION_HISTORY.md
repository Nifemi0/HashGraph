## Version History

### v1.0.4
- Default explorer API to `hsk.blockscout.com` (no 301 hop)
- Point simulate / read / proxy bytecode RPCs at HashKey **Mainnet** (chain id 177)
- Keep on-chain registry attestations on testnet (configurable via `REGISTRY_*`)
- HashKey demo seed addresses (CELA, WHSK, DOGS, Cellula, KycGate)
- Demo smoke script + demo kit (`DEMO.md`, end card)
- Landing/explorer demo chips fixed to verified mainnet contracts
- Site dark-mode / docs polish included in this release train

### v1.0.3
- Previous npm release (MCP server package)

Use `git tag v1.0.4` after publish to pin this train.
